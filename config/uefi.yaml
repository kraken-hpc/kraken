# kraken-build.go: describes a build for embedding in UEFI firmware
targets:
  'u-root':

# included extensions
extensions:
  - github.com/hpc/kraken/extensions/IPv4

# included modules
modules:
  - github.com/hpc/kraken/modules/restapi
