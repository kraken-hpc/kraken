// One-time generated by kraken {{ .Global.Version }}. 
// `kraken module update` *will not* replace this file.
// `kraken -f module generate` *will* replace this file.
// You probably want to edit this file to add values you need.

syntax = "proto3";
package {{ .ProtoPackage }};
option go_package = ".;{{ .PackageName }}";

import "github.com/gogo/protobuf/gogoproto/gogo.proto";
option (gogoproto.marshaler_all) = true;
option (gogoproto.unmarshaler_all) = true;
option (gogoproto.sizer_all) = true;
option (gogoproto.goproto_registration) = true;
option (gogoproto.messagename_all) = true;

// TODO: (optional) Define sub-messages here

message {{ .Name }} { /* Extension */
    // TODO: Add variables here
    {{- range .CustomTypes }}
    // To define a variable for custom type {{ . }}:
    // bytes my{{ . }}      = 2 [(gogoproto.customtype) = "{{ $.PackageUrl }}/customtypes.{{ . }};
    {{- end }}
}
